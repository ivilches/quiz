<div id="lista_preguntas">
	<% if(quizes.length) { %>
	<ul>
		<%  for (var i=0; i < quizes.length; i++){  %>
		 
			<li> 
				<span class="pregunta_lista"><a href="quizes/<%= quizes[i].id %>"> <%= quizes[i].pregunta %> </a> </span>
				<span><a href="quizes/<%= quizes[i].id %>/edit" class="enlace_boton">editar</a></span>
				<form method="post" action="quizes/<%= quizes[i].id %>?_method=delete">
					<input type="submit" onclick="return confirm('¿Borrar: <%= quizes[i].pregunta %>?');" value="eliminar" />
				</form>
			</li>
		<% } %>
		</ul>
	<% } 
	 else { %>
	<span>No hay preguntas referentes a su búsqueda</span>	
	<% } %>
</div>

<div id="form_busqueda">
	<form method="get" action="/quizes" >
		<input type="text" name="search" placeholder="Buscar pregunta" />
		<input type="submit" value="Buscar" />
	</form>
</div>
<div>
	<a href="/quizes/new"><button>Crear pregunta</button></a>
</div>

